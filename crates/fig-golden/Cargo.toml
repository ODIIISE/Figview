[package]
name = "fig-golden"
version = "0.1.0"
edition = "2021"
description = "Golden-reference renderer and CLI toolkit: inspect / render / svg for .fig documents"

[dependencies]
fig-parser = { path = "../fig-parser" }
fig-renderer = { path = "../fig-renderer" }
resvg = "0.45"
serde_json = "1"
